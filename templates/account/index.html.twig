{% extends 'base.html.twig' %}

{% block title %}Mon compte - La boutique vitaminée{% endblock %}

{% block content %}
<h1>Mon compte</h1>
    {% if is_granted('ROLE_ADMIN') %}
        <small><a href="/admin">Vous êtes un administrateur, accéder au back-office<a/></small><br>
    {% endif %}
    Bienvenue sur votre compte {{ app.user.firstname }} ! <br>
    C'est dans cet espace que vous allez pouvoir gérer toutes vos informations personnelles.
    <hr>
        <div class="row">
            <div class="col-md-4">
                <div class="account-item text-center">
                    <img src="{{ asset('assets/img/padlock.png') }}" alt="Modifier mon mot de passe"><br>
                    <a href="{{ path('account_password') }}">Modifier mon mot de passe<a/>
                </div>
            </div>
            <div class="col-md-4">
                <div class="account-item text-center">
                    <img src="{{ asset('assets/img/phone-book.png') }}" alt="Gérer mes adresses"><br>
                    <a href="{{ path('account_address_add') }}">Ajouter mes adresses</a>
                </div>
            </div>
            <div class="col-md-4">
                <div class="account-item text-center">
                    <img src="{{ asset('assets/img/cart.png') }}" alt="Mes commandes"><br>
                    <a href="{{ path('account_order') }}">Mes commandes</a>
                </div>
            </div>
        </div>
    <hr>

{% endblock %}
